<!-- <div class="item"> -->
  <% 
  var useCollectionIntroTest = typeof contentEnitity.contenttypeattributes === 'undefined' || (contentEnitity.contenttypeattributes['periodical-collection'] && contentEnitity.contenttypeattributes['periodical-collection']['use-collection-intro']!=='false');
  if(useCollectionIntroTest){ %>
  <div id="container" class="container intro-effect-fadeout modify item">
    <header class="header">
      <% include ../shared/partial/_contentEntity_bgImg %>  
      <div class="title">
        <% if(typeof linkCollectionTitle !== 'undefined'){ %>
          <h1><a href="/collection/<%- contentEnitity.name%>"><%- contentEnitity.title  %></a></h1>
        <% }
        else{%>
          <h1><%- contentEnitity.title  %></h1>
        <%}%>
        <% if(typeof contentEnitity.contenttypeattributes !== 'undefined'){ %>
          <p class="subline"><%- contentEnitity.contenttypeattributes['periodical-collection'].dek %></p>
        <% }%>
        <% var metaauthor = contentEnitity.primaryauthor;
          var metadate = contentEnitity.createdat; %>
        <% include ../shared/partial/_itemmeta %>
      </div>
    </header>
    <button class="trigger" data-info="Click to read more"><span>↓</span></button>
    <article class="content">
    <% if(typeof showCollectionContent === 'undefined' || showCollectionContent === true){ %>
      <section class="collection-content">
        <%- contentEnitity.content %>
      </section>
    <% }%>
  <% }%>

      <div id="list-view-container" class="list-view-scroll-container">
    		<div id="list-view-scroll" class="list-view-scroll">
          <% 
          var iterateObjectArray = collection.items || layoutdata.items;
          if(iterateObjectArray && iterateObjectArray.length>0){
            iterateObjectArray.forEach(function(itemObj,index){
              var contentEnitity = itemObj.item || itemObj;
                console.log('contentEnitity in array',contentEnitity);

              if(contentEnitity){
                contentContentAttributes = (typeof contentEnitity.contenttypeattributes !== 'undefined' && contentEnitity.contenttypeattributes.periodical)? true : false; 
                contentBgImgUseDefault = true;
                contentTitleLink = true;
                main_content_side=(index%2==0) ? "left":"right";
                main_media_side=(index%2==0) ? "right":"left";%>
                <section id="list-view-scroll-section-<%- (index+1) %>" class="list-view-scroll-section list-view-scroll">
                  <% if (main_content_side=="left"){%>
                    <% include _collectionListViewScroll_article %>
                    <% include _collectionListViewScroll_figure %>
                    <%
                    }
                    else{%>
                  <% include _collectionListViewScroll_figure %>
                  <% include _collectionListViewScroll_article %>
                  <%}%>
                </section>
              <%}
            });
          }%>
        </div>
    	</div>
    <%
    if(useCollectionIntroTest){
      %>
    </article>
  <%}%>
<!-- </div> -->