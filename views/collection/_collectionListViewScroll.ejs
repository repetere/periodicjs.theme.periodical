<div class="item">
<% //console.log("contentEnitity.contenttypeattributes['periodical-collection']",contentEnitity.contenttypeattributes['periodical-collection']) %>
  <% if(contentEnitity.contenttypeattributes && contentEnitity.contenttypeattributes['periodical-collection'] && contentEnitity.contenttypeattributes['periodical-collection']['use-collection-intro']=='true'){ %>
  <div id="container" class="container intro-effect-fadeout modify">
    <header class="header">
      <% include ../shared/partial/_contentEntity_bgImg %>  
      <div class="title">
        <h1><%- contentEnitity.title  %></h1>
        <p class="subline"><%- contentEnitity.contenttypeattributes['periodical-collection'].dek %></p>
        <% var metaauthor = contentEnitity.primaryauthor;
          var metadate = contentEnitity.createdat; %>
        <% include ../shared/partial/_itemmeta %>
      </div>
    </header>
    <button class="trigger" data-info="Click to read more"><span>↓</span></button>
    <article class="content">
      <section class="collection-content">
        <%- contentEnitity.content %>
      </section>
  <% }%>

      <div id="list-view-container" class="list-view-scroll-container">
    		<div id="list-view-scroll" class="list-view-scroll">
          <% if(collection.items && collection.items.length>0){
            collection.items.forEach(function(itemObj,index){%>
            <% var contentEnitity = itemObj.item;
                contentContentAttributes = (contentEnitity.contenttypeattributes && contentEnitity.contenttypeattributes.periodical)? true : false; 
                contentBgImgUseDefault = true;
                contentTitleLink = true;
                main_content_side=(index%2==0) ? "left":"right";
                main_media_side=(index%2==0) ? "right":"left";%>
              <section id="list-view-scroll-section-<%- (index+1) %>" class="list-view-scroll-section list-view-scroll">
                <% if (main_content_side=="left"){%>
                  <% include _collectionListViewScroll_article %>
                  <% include _collectionListViewScroll_figure %>
                  <%
                  }
                  else{%>
                <% include _collectionListViewScroll_figure %>
                <% include _collectionListViewScroll_article %>
                <%}%>
              </section>
            <%});
          }%>
        </div>
    	</div>
    <%
    if(contentEnitity.contenttypeattributes && contentEnitity.contenttypeattributes['periodical-collection'] && contentEnitity.contenttypeattributes['periodical-collection'].useintro){
      %>
    </article>
<!--   </div>
 -->  <%}%>
</div>